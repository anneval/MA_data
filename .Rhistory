pred <- cbind(Date2,pred)
View(pred)
pred <- cbind(Date_q,pred)
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[i] <- predict(expanding_OLS[i],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i]- data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[i] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
#predicted_OLS[i] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OL[i] <- resid(expanding_OLS[[i]])
#predicted_OLS[i] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[i] <- resid(expanding_OLS[[i]])
#predicted_OLS[i] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
mod2$residuals
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
#predicted_OLS[i] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict(expanding_OLS,newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
View(expanding_OLS)
?predict
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
View(expanding_OLS)
51+13
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_OLS <- expanding_window_OLS(Data[,1], dep_var, start = 13)
nrow(Data[,1])
nrow(Data)
nrow(Data[,1])
nrow(Data[,"gdp"])
nrow(Data[,gdp])
View(Data)
nrow(Data)
View(Data)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,1])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t:nrow(data),1])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,2])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
error_OLS_model <- function(data, expanding_OLS, indep_var, start = 13){
error_OLS <- c()
#predicted_OLS <- list() # empty vector
j <- 0
for(i in start:nrow(data)){
j <- j+1
# geht durch alle 12-1 rows!
expanding_OLS <- lm(formula = formula_ols_all_rev, data = data[1:(i-1),])
predicted_OLS  <- predict.lm(expanding_OLS, newdata = data[i,])# hier forecst v<irablr??
# trying to predict value i
error_OLS[j] <- as.numeric(predicted_OLS - data[i,]) # second is predcted vlaue ! FORECASTING !!! when I include GDP_lead butthen I also have tom do it in the formula !
}
return(error_OLS)
# return(predicted_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
expanding_window_OLS <- function(data, dep_var, start = 12){ # change to 12
expanding_OLS <- list() # empty vector
residuals <- list() # empty vector
j <- 0
for(i in start:nrow(data)){
j <- j+1
expanding_OLS[[j]] <- lm(formula = formula_ols_all_rev, data = data[1:(i-1),])
}
return(expanding_OLS)
}
error_OLS_model <- function(data, expanding_OLS, indep_var, start = 13){
error_OLS <- c()
#predicted_OLS <- list() # empty vector
j <- 0
for(i in start:nrow(data)){
j <- j+1
# geht durch alle 12-1 rows!
expanding_OLS <- lm(formula = formula_ols_all_rev, data = data[1:(i-1),])
predicted_OLS  <- predict.lm(expanding_OLS, newdata = data[i,])# hier forecst v<irablr??
# trying to predict value i
error_OLS[j] <- as.numeric(predicted_OLS - data[i,]) # second is predcted vlaue ! FORECASTING !!! when I include GDP_lead butthen I also have tom do it in the formula !
}
return(error_OLS)
# return(predicted_OLS)
}
dep_var <- "gdp"
expanding_OLS <- expanding_window_OLS(Data)
expanding_window_OLS <- function(data, dep_var, start = 12){ # change to 12
expanding_OLS <- list() # empty vector
residuals <- list() # empty vector
j <- 0
for(i in start:nrow(data)){
j <- j+1
expanding_OLS[[j]] <- lm(formula = model, data = data[1:(i-1),])
}
return(expanding_OLS)
}
error_OLS_model <- function(data, expanding_OLS, indep_var, start = 13){
error_OLS <- c()
#predicted_OLS <- list() # empty vector
j <- 0
for(i in start:nrow(data)){
j <- j+1
# geht durch alle 12-1 rows!
expanding_OLS <- lm(formula = model, data = data[1:(i-1),])
predicted_OLS  <- predict.lm(expanding_OLS, newdata = data[i,])# hier forecst v<irablr??
# trying to predict value i
error_OLS[j] <- as.numeric(predicted_OLS - data[i,]) # second is predcted vlaue ! FORECASTING !!! when I include GDP_lead butthen I also have tom do it in the formula !
}
return(error_OLS)
# return(predicted_OLS)
}
expanding_OLS <- expanding_window_OLS(Data)
View(Data)
expanding_OLS <- expanding_window_OLS(Data[,1:3])
test <- error_OLS_model()
test <- error_OLS_model(Data[,1:3],expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS)
#predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(forecasts)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS)
#predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
dep_var <- "gdp"
model = gdp ~ lag(gdp)
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[i])
#predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
#predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
mod5<- lm(gdp ~ lag(gdp) + google, data = Data_train)
pred_mod5 <- predict(mod5, Data_test)
summary(mod5)
accuracy(mod5)
accuracy(pred_mod5)
pred_mod5 <- predict(mod5, Data_test)
pred_mod5
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data)#[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[1:(t-1),]))#[t,])
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[1:t,]))#[t,])
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[1:t,])#[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t-1:t,])#[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
expanding_window_OLS <- function(data, dep_var, start = 13){ # change to 12
expanding_OLS <- list() # empty vector
predicted_OLS <- list()
resid_OLS <- list()
error_OLS <- c()
i <- 0
for(t in start:nrow(data)){ #t in 13:51
i <- i+1
expanding_OLS[[i]] <- lm(formula = model, data = data[1:(t-1),]) # start at 13-1 so 12!
resid_OLS[[i]] <- resid(expanding_OLS[[i]])
predicted_OLS[[i]] <- predict.lm(expanding_OLS[[i]],newdata = data[t-1:t,])#[t,])
# error_OLS[i] <- as.numeric(predicted_OLS[i] - data[t,]) # anpassen je nach dem welchs
Summary_OLS <- list(expanding_OLS ,predicted_OLS,resid_OLS,error_OLS)
}
return(Summary_OLS)
}
expanding_OLS <- expanding_window_OLS(Data, dep_var, start = 13)
View(expanding_OLS)
View(expanding_OLS)
